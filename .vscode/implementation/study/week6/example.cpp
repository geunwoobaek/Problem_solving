#include <bits/stdc++.h>
using namespace std;
map<string, int> dic;
vector<vector<int>> vec;
int main()
{
    vector<string> str;
    for (auto& s : str)
    {
        if (dic[s] == 0)
        dic[s]++;
    }
    
}