#include <bits/stdc++.h>
using namespace std;
int testCase, N, M;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    cin >> testCase;
    while (testCase-- > 0)
    {
        cin >> N >> M;
        while (M-- > 0)
        {
            
        }
    }
}